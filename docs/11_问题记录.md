## 该版本需改进部分

+ assert 修改为 判断返回值
+ C++11，new delete 修改为智能指针
+ 利用RAII 解决死锁问题
+ 判断容器为空，不要用 size 判断，用 mt 判断
+ 抛异常部分修改为打印错误退出
+ 尽量不要用 detach
+ one thread one loop 而不是线程池的方式
+ try catch C++ 很少用
+ http 解包




  